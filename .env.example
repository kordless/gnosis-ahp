# This file is an example. Copy it to .env and fill in your actual values.
# Do NOT commit the .env file to version control.

# The pre-shared key that clients must provide to the /?f=auth endpoint
# to receive a temporary bearer token.
AHP_TOKEN="your-strong-pre-shared-key"

# The host and port the server will listen on.
HOST="0.0.0.0"
PORT="8080"
